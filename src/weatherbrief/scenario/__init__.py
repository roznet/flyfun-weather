"""Scenario previews — overnight 'where/when could I fly' briefings.

Phase 0 (issue #225) ships only the measurement harness in ``measure.py``.
No scheduler, no DB model, no UI — those land in #226 once the budget numbers
from the harness are known.
"""
